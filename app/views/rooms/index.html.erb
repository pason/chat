<p style="color: green"><%= notice %></p>

<h1>Channels</h1>

<div id="rooms">
  <% @rooms.each do |room| %>
    <%= render room %>
    <p>
      <%= link_to "Open this channel", room %>
    </p>
  <% end %>
</div>

<%= link_to "New channel", new_room_path %>
